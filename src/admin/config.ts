/**
 * Admin UI metadata (no secrets). Extend for feature flags or role hints later.
 */
export const CMS_ADMIN = {
  name: "Abexis CMS",
  /** Used in layouts / nav */
  shortTitle: "CMS",
} as const;
