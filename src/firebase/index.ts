/**
 * Browser Firebase SDK (`"use client"` in `./client`).
 *
 * - Server / Admin: `import { … } from "@/firebase/server"`.
 * - Env types / parse helpers (no `"use client"`): `import { … } from "@/firebase/env.schema"`.
 * - Thin helpers: `@/firebase/auth`, `@/firebase/firestore`, `@/firebase/storage`.
 */
export * from "./client";
